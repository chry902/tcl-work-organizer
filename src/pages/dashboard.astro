---
import Layout from "@/layouts/Layout.astro";
import WorksPanel from "@/components/reactComponent/works/WorksPanel.jsx";
const auth = Astro.locals.auth();
if (!auth?.userId) {
  return Astro.redirect("/sign-in");
}
---
<Layout>
<div class="flex flex-col items-center justify-center w-dvw p-6 gap-4">
    <h1>Dashboard</h1>
  <p>Se vedi questa pagina, sei loggato ✅</p>
<a href="/">Torna alla home</a>
</div>
  <WorksPanel client:load/>
</Layout>